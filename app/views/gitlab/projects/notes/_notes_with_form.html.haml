%ul#notes-list.notes.main-notes-list
  = render "gitlab/projects/notes/notes"
.js-notes-busy

.js-main-target-form
- if can? current_user, :write_note, @project
  = render "gitlab/projects/notes/form"

:javascript
  new Notes("#{project_notes_path(target_id: @noteable.id, target_type: @noteable.class.name.underscore.sub(/^gitlab\//, ''))}", #{@notes.map(&:id).to_json}, #{Time.now.to_i})
