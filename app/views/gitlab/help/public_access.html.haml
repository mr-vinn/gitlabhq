= render layout: 'gitlab/help/layout' do
  %h3.page-title Public Access 

  .help_body
    = preserve do
      = markdown File.read(Gitlab::Engine.root.join("doc", "public_access", "public_access.md"))
