= render layout: 'gitlab/help/api_layout' do
  %h3.page-title
    %span.light API
    %span
      \/
    = @category.titleize

  .file-holder
    .file-title
      %i.icon-file
      = @category
    .file-content.wiki
      = preserve do
        = markdown File.read(Gitlab::Engine.root.join("doc", "api", "#{@category}.md"))
